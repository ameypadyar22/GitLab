name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Display Message
        run: echo Continuous Integration Pipeline Executed Successfully

      - name: Display Repository Files
        run: dir